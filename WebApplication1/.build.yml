parameters:
  name: ''
  queue: ''

phases:
- phase: ${{ parameters.name }}
  queue: ${{ parameters.queue }}
  steps:
  - task: UseDotNet@2
      inputs:
        packageType: 'sdk'
        useGlobalJson: true

